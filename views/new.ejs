<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Writing New Chat</title>
    <style>
        body {
            background-color: #fff8e1;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        form {
            background-color: #fff3e0;
            padding: 30px;
            border-radius: 16px;
            border: 2px solid #fbc02d;
            box-shadow: 0 10px 20px rgba(245, 124, 0, 0.2);
            display: flex;
            flex-direction: column;
            width: 90%;
            max-width: 400px;
        }

        h3 {
            text-align: center;
            color: #f57c00;
            margin-bottom: 20px;
            font-size: 1.8rem;
        }

        input,
        textarea {
            padding: 12px;
            margin-bottom: 15px;
            border: 1px solid #fbc02d;
            border-radius: 8px;
            font-size: 1rem;
            background-color: #fffde7;
        }

        textarea {
            resize: vertical;
            min-height: 100px;
        }

        button {
            padding: 12px;
            background-color: #f57c00;
            color: white;
            font-size: 1rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        button:hover {
            background-color: #e65100;
            transform: scale(1.05);
        }
    </style>
</head>

<body>
    <form action="/chats" method="POST">
        <h3>Write New Chat..</h3>
        <input name="from" placeholder="Who are you?" required>
        <textarea name="msg" placeholder="Write your message..." required></textarea>
        <input placeholder="Add receiver" name="to" required>
        <button>Create Chat</button>
    </form>
</body>

</html>

</html>